name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name : print greet
        run : echo "Hello World..!!"
      - name : print good bye
        run : echo "Done bye..!!"
